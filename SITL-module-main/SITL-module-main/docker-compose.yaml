services:
  zookeeper:
    profiles: ["kafka"]
    image: confluentinc/cp-zookeeper:7.5.0
    container_name: sitl-zookeeper
    environment:
      ZOOKEEPER_CLIENT_PORT: 2181
      ZOOKEEPER_TICK_TIME: 2000
    healthcheck:
      test: ["CMD", "nc", "-z", "localhost", "2181"]
      interval: 10s
      timeout: 5s
      retries: 5

  kafka:
    profiles: ["kafka"]
    image: confluentinc/cp-kafka:7.5.0
    container_name: sitl-kafka
    depends_on:
      zookeeper:
        condition: service_healthy
    ports:
      - "9092:9092"
    environment:
      KAFKA_BROKER_ID: 1
      KAFKA_ZOOKEEPER_CONNECT: zookeeper:2181
      KAFKA_LISTENERS: PLAINTEXT://0.0.0.0:29092,PLAINTEXT_HOST://0.0.0.0:9092
      KAFKA_ADVERTISED_LISTENERS: PLAINTEXT://kafka:29092,PLAINTEXT_HOST://localhost:9092
      KAFKA_LISTENER_SECURITY_PROTOCOL_MAP: PLAINTEXT:PLAINTEXT,PLAINTEXT_HOST:PLAINTEXT
      KAFKA_INTER_BROKER_LISTENER_NAME: PLAINTEXT
      KAFKA_OFFSETS_TOPIC_REPLICATION_FACTOR: 1
      KAFKA_AUTO_CREATE_TOPICS_ENABLE: "true"
    healthcheck:
      test: ["CMD", "kafka-broker-api-versions", "--bootstrap-server", "localhost:9092"]
      interval: 10s
      timeout: 10s
      retries: 5

  mosquitto:
    profiles: ["mqtt"]
    image: eclipse-mosquitto:2.0
    container_name: sitl-mosquitto
    ports:
      - "1883:1883"
    volumes:
      - ./mosquitto.conf:/mosquitto/config/mosquitto.conf:ro
    healthcheck:
      test: ["CMD", "mosquitto_sub", "-h", "localhost", "-p", "1883", "-t", "$$SYS/broker/version", "-C", "1", "-W", "5"]
      interval: 10s
      timeout: 10s
      retries: 5

  # Имя сервиса sitl_redis — не «redis»: в общей сети drones_net уже есть другие Redis (GCS, DronePort),
  # и hostname «redis» резолвится в несколько IP; клиенты SITL попадали не в sitl-redis.
  sitl_redis:
    image: redis:7-alpine
    container_name: sitl-redis
    # Порт 6379 не публикуем на хост (конфликт с другим Redis на хосте).
    command: redis-server --appendonly yes
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 5s
      retries: 5

  verifier:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: sitl-verifier
    command: ["python", "-u", "src/verifier.py"]
    environment:
      BROKER_BACKEND: ${BROKER_BACKEND:-kafka}
      KAFKA_SERVERS: ${KAFKA_SERVERS:-kafka:29092}
      MQTT_HOST: ${MQTT_HOST:-mosquitto}
      MQTT_PORT: ${MQTT_PORT:-1883}
      MQTT_USERNAME: ${MQTT_USERNAME:-}
      MQTT_PASSWORD: ${MQTT_PASSWORD:-}
      MQTT_QOS: ${MQTT_QOS:-1}
      INPUT_TOPICS: ${INPUT_TOPICS:-sitl.commands,sitl-drone-home}
      COMMAND_TOPIC: ${COMMAND_TOPIC:-sitl.commands}
      HOME_TOPIC: ${HOME_TOPIC:-sitl-drone-home}
      VERIFIED_COMMAND_TOPIC: ${VERIFIED_COMMAND_TOPIC:-sitl.verified-commands}
      VERIFIED_HOME_TOPIC: ${VERIFIED_HOME_TOPIC:-sitl.verified-home}
    restart: unless-stopped

  controller:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: sitl-controller
    command: ["python", "-u", "src/controller.py"]
    depends_on:
      sitl_redis:
        condition: service_healthy
    environment:
      BROKER_BACKEND: ${BROKER_BACKEND:-kafka}
      KAFKA_SERVERS: ${KAFKA_SERVERS:-kafka:29092}
      MQTT_HOST: ${MQTT_HOST:-mosquitto}
      MQTT_PORT: ${MQTT_PORT:-1883}
      MQTT_USERNAME: ${MQTT_USERNAME:-}
      MQTT_PASSWORD: ${MQTT_PASSWORD:-}
      MQTT_QOS: ${MQTT_QOS:-1}
      REDIS_URL: ${REDIS_URL:-redis://sitl_redis:6379}
      VERIFIED_COMMAND_TOPIC: ${VERIFIED_COMMAND_TOPIC:-sitl.verified-commands}
      VERIFIED_HOME_TOPIC: ${VERIFIED_HOME_TOPIC:-sitl.verified-home}
      STATE_TTL_SEC: ${STATE_TTL_SEC:-7200}
    restart: unless-stopped

  core:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: sitl-core
    command: ["python", "-u", "src/core.py"]
    depends_on:
      sitl_redis:
        condition: service_healthy
    environment:
      REDIS_URL: ${REDIS_URL:-redis://sitl_redis:6379}
      UPDATE_FREQUENCY_HZ: ${UPDATE_FREQUENCY_HZ:-10.0}
      STATE_TTL_SEC: ${STATE_TTL_SEC:-7200}
    restart: unless-stopped

  messaging:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: sitl-messaging
    command: ["python", "-u", "src/messaging.py"]
    depends_on:
      sitl_redis:
        condition: service_healthy
    environment:
      BROKER_BACKEND: ${BROKER_BACKEND:-kafka}
      KAFKA_SERVERS: ${KAFKA_SERVERS:-kafka:29092}
      MQTT_HOST: ${MQTT_HOST:-mosquitto}
      MQTT_PORT: ${MQTT_PORT:-1883}
      MQTT_USERNAME: ${MQTT_USERNAME:-}
      MQTT_PASSWORD: ${MQTT_PASSWORD:-}
      MQTT_QOS: ${MQTT_QOS:-1}
      REDIS_URL: ${REDIS_URL:-redis://sitl_redis:6379}
      POSITION_REQUEST_TOPIC: ${POSITION_REQUEST_TOPIC:-sitl.telemetry.request}
      POSITION_RESPONSE_TOPIC: ${POSITION_RESPONSE_TOPIC:-sitl.telemetry.response}
    restart: unless-stopped

volumes:
  redis_data:

# Внешняя сеть: тот же drones_net, что у Mosquitto (make sitl-net в Makefile SITL)
networks:
  default:
    external: true
    name: ${DOCKER_NETWORK:-drones_net}
