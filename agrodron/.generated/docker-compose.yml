# AUTO-GENERATED by scripts/prepare_system.py
# Do not edit manually. Re-run: python scripts/prepare_system.py agrodron
name: drones
services:
  kafka:
    image: apache/kafka:latest
    container_name: kafka
    profiles:
    - kafka
    ports:
    - ${KAFKA_PORT:-9092}:9092
    - ${KAFKA_INTERNAL_PORT:-29092}:29092
    environment:
      KAFKA_NODE_ID: 1
      KAFKA_PROCESS_ROLES: broker,controller
      KAFKA_LISTENERS: SASL_PLAINTEXT://0.0.0.0:9092,SASL_PLAINTEXT_INTERNAL://0.0.0.0:29092,CONTROLLER://0.0.0.0:9093
      KAFKA_ADVERTISED_LISTENERS: SASL_PLAINTEXT://localhost:${KAFKA_PORT:-9092},SASL_PLAINTEXT_INTERNAL://kafka:29092
      KAFKA_LISTENER_SECURITY_PROTOCOL_MAP: CONTROLLER:PLAINTEXT,SASL_PLAINTEXT:SASL_PLAINTEXT,SASL_PLAINTEXT_INTERNAL:SASL_PLAINTEXT
      KAFKA_INTER_BROKER_LISTENER_NAME: SASL_PLAINTEXT_INTERNAL
      KAFKA_SASL_MECHANISM_INTER_BROKER_PROTOCOL: PLAIN
      KAFKA_SASL_ENABLED_MECHANISMS: PLAIN
      KAFKA_CONTROLLER_LISTENER_NAMES: CONTROLLER
      KAFKA_CONTROLLER_QUORUM_VOTERS: 1@localhost:9093
      KAFKA_OFFSETS_TOPIC_REPLICATION_FACTOR: 1
      KAFKA_LOG_DIRS: /tmp/kraft-combined-logs
      CLUSTER_ID: MkU3OEVBNTcwNTJENDM2Qk
      KAFKA_OPTS: -Djava.security.auth.login.config=/tmp/jaas.conf
      ADMIN_USER: ${ADMIN_USER:-admin}
      ADMIN_PASSWORD: ${ADMIN_PASSWORD:-admin123}
      COMPONENT_USER_A: ${COMPONENT_USER_A:-}
      COMPONENT_PASSWORD_A: ${COMPONENT_PASSWORD_A:-}
      COMPONENT_USER_B: ${COMPONENT_USER_B:-}
      COMPONENT_PASSWORD_B: ${COMPONENT_PASSWORD_B:-}
      COMPONENT_USER_C: ${COMPONENT_USER_C:-}
      COMPONENT_PASSWORD_C: ${COMPONENT_PASSWORD_C:-}
      COMPONENT_USER_D: ${COMPONENT_USER_D:-}
      COMPONENT_PASSWORD_D: ${COMPONENT_PASSWORD_D:-}
      COMPONENT_USER_E: ${COMPONENT_USER_E:-}
      COMPONENT_PASSWORD_E: ${COMPONENT_PASSWORD_E:-}
      COMPONENT_USER_F: ${COMPONENT_USER_F:-}
      COMPONENT_PASSWORD_F: ${COMPONENT_PASSWORD_F:-}
      COMPONENT_USER_G: ${COMPONENT_USER_G:-}
      COMPONENT_PASSWORD_G: ${COMPONENT_PASSWORD_G:-}
      COMPONENT_USER_H: ${COMPONENT_USER_H:-}
      COMPONENT_PASSWORD_H: ${COMPONENT_PASSWORD_H:-}
      COMPONENT_USER_I: ${COMPONENT_USER_I:-}
      COMPONENT_PASSWORD_I: ${COMPONENT_PASSWORD_I:-}
      COMPONENT_USER_J: ${COMPONENT_USER_J:-}
      COMPONENT_PASSWORD_J: ${COMPONENT_PASSWORD_J:-}
      COMPONENT_USER_K: ${COMPONENT_USER_K:-}
      COMPONENT_PASSWORD_K: ${COMPONENT_PASSWORD_K:-}
    volumes:
    - ../../docker/kafka/entrypoint.sh:/entrypoint.sh:ro
    command:
    - /bin/sh
    - /entrypoint.sh
    networks:
    - drones_net
    healthcheck:
      test:
      - CMD-SHELL
      - pgrep -f java || exit 1
      interval: 10s
      timeout: 5s
      retries: 8
      start_period: 60s
    restart: unless-stopped
  mosquitto:
    image: eclipse-mosquitto:2
    container_name: mosquitto
    user: 0:0
    profiles:
    - mqtt
    ports:
    - ${MQTT_PORT:-1883}:1883
    environment:
      ADMIN_USER: ${ADMIN_USER:-admin}
      ADMIN_PASSWORD: ${ADMIN_PASSWORD:-admin123}
      COMPONENT_USER_A: ${COMPONENT_USER_A:-}
      COMPONENT_PASSWORD_A: ${COMPONENT_PASSWORD_A:-}
      COMPONENT_USER_B: ${COMPONENT_USER_B:-}
      COMPONENT_PASSWORD_B: ${COMPONENT_PASSWORD_B:-}
      COMPONENT_USER_C: ${COMPONENT_USER_C:-}
      COMPONENT_PASSWORD_C: ${COMPONENT_PASSWORD_C:-}
      COMPONENT_USER_D: ${COMPONENT_USER_D:-}
      COMPONENT_PASSWORD_D: ${COMPONENT_PASSWORD_D:-}
      COMPONENT_USER_E: ${COMPONENT_USER_E:-}
      COMPONENT_PASSWORD_E: ${COMPONENT_PASSWORD_E:-}
      COMPONENT_USER_F: ${COMPONENT_USER_F:-}
      COMPONENT_PASSWORD_F: ${COMPONENT_PASSWORD_F:-}
      COMPONENT_USER_G: ${COMPONENT_USER_G:-}
      COMPONENT_PASSWORD_G: ${COMPONENT_PASSWORD_G:-}
      COMPONENT_USER_H: ${COMPONENT_USER_H:-}
      COMPONENT_PASSWORD_H: ${COMPONENT_PASSWORD_H:-}
      COMPONENT_USER_I: ${COMPONENT_USER_I:-}
      COMPONENT_PASSWORD_I: ${COMPONENT_PASSWORD_I:-}
      COMPONENT_USER_J: ${COMPONENT_USER_J:-}
      COMPONENT_PASSWORD_J: ${COMPONENT_PASSWORD_J:-}
      COMPONENT_USER_K: ${COMPONENT_USER_K:-}
      COMPONENT_PASSWORD_K: ${COMPONENT_PASSWORD_K:-}
    volumes:
    - ../../docker/mqtt/mosquitto.conf:/mosquitto/config/mosquitto.conf:ro
    - ../../docker/mqtt/entrypoint.sh:/entrypoint.sh:ro
    command:
    - /bin/sh
    - /entrypoint.sh
    networks:
    - drones_net
    healthcheck:
      test:
      - CMD-SHELL
      - mosquitto_pub -h localhost -u $$ADMIN_USER -P $$ADMIN_PASSWORD -t healthcheck
        -m ok
      interval: 10s
      timeout: 5s
      retries: 3
      start_period: 5s
    restart: unless-stopped
  security_monitor:
    build:
      context: ../..
      dockerfile: agrodron/components/security_monitor/docker/Dockerfile
    profiles:
    - kafka
    - mqtt
    environment:
      TOPIC_VERSION: ${TOPIC_VERSION:-v1}
      SYSTEM_NAME: ${SYSTEM_NAME:-Agrodron}
      INSTANCE_ID: ${INSTANCE_ID:-Agrodron001}
      MQTT_BUS_CALLBACK_WORKERS: ${MQTT_BUS_CALLBACK_WORKERS:-32}
      BROKER_TYPE: ${BROKER_TYPE:-mqtt}
      MQTT_BROKER: ${MQTT_BROKER:-mosquitto}
      MQTT_PORT: ${MQTT_PORT:-1883}
      KAFKA_BOOTSTRAP_SERVERS: kafka:29092
      BROKER_USER: ${ADMIN_USER:-admin}
      BROKER_PASSWORD: ${ADMIN_PASSWORD:-admin123}
      PYTHONUNBUFFERED: 1
      ORVD_TOPIC: ${ORVD_TOPIC:-}
      NUS_TOPIC: ${NUS_TOPIC:-}
      DRONEPORT_TOPIC: ${DRONEPORT_TOPIC:-}
      SITL_TOPIC: ${SITL_TOPIC:-}
      SITL_COMMANDS_TOPIC: ${SITL_COMMANDS_TOPIC:-}
      SITL_TELEMETRY_REQUEST_TOPIC: ${SITL_TELEMETRY_REQUEST_TOPIC:-}
      COMPONENT_ID: ${SECURITY_MONITOR_COMPONENT_ID:-security_monitor}
      POLICY_ADMIN_SENDER: ${SECURITY_MONITOR_POLICY_ADMIN_SENDER:-}
      SECURITY_POLICIES: ${SECURITY_MONITOR_SECURITY_POLICIES:-}
      SECURITY_MONITOR_PROXY_REQUEST_TIMEOUT_S: ${SECURITY_MONITOR_SECURITY_MONITOR_PROXY_REQUEST_TIMEOUT_S:-10.0}
    networks:
    - drones_net
    restart: unless-stopped
    depends_on:
      kafka:
        condition: service_healthy
        required: false
      mosquitto:
        condition: service_healthy
        required: false
  journal:
    build:
      context: ../..
      dockerfile: agrodron/components/journal/docker/Dockerfile
    profiles:
    - kafka
    - mqtt
    environment:
      TOPIC_VERSION: ${TOPIC_VERSION:-v1}
      SYSTEM_NAME: ${SYSTEM_NAME:-Agrodron}
      INSTANCE_ID: ${INSTANCE_ID:-Agrodron001}
      MQTT_BUS_CALLBACK_WORKERS: ${MQTT_BUS_CALLBACK_WORKERS:-32}
      BROKER_TYPE: ${BROKER_TYPE:-mqtt}
      MQTT_BROKER: ${MQTT_BROKER:-mosquitto}
      MQTT_PORT: ${MQTT_PORT:-1883}
      KAFKA_BOOTSTRAP_SERVERS: kafka:29092
      BROKER_USER: ${ADMIN_USER:-admin}
      BROKER_PASSWORD: ${ADMIN_PASSWORD:-admin123}
      PYTHONUNBUFFERED: 1
      ORVD_TOPIC: ${ORVD_TOPIC:-}
      NUS_TOPIC: ${NUS_TOPIC:-}
      DRONEPORT_TOPIC: ${DRONEPORT_TOPIC:-}
      SITL_TOPIC: ${SITL_TOPIC:-}
      SITL_COMMANDS_TOPIC: ${SITL_COMMANDS_TOPIC:-}
      SITL_TELEMETRY_REQUEST_TOPIC: ${SITL_TELEMETRY_REQUEST_TOPIC:-}
      COMPONENT_ID: ${JOURNAL_COMPONENT_ID:-journal}
    networks:
    - drones_net
    restart: unless-stopped
    depends_on:
      kafka:
        condition: service_healthy
        required: false
      mosquitto:
        condition: service_healthy
        required: false
  navigation:
    build:
      context: ../..
      dockerfile: agrodron/components/navigation/docker/Dockerfile
    profiles:
    - kafka
    - mqtt
    environment:
      TOPIC_VERSION: ${TOPIC_VERSION:-v1}
      SYSTEM_NAME: ${SYSTEM_NAME:-Agrodron}
      INSTANCE_ID: ${INSTANCE_ID:-Agrodron001}
      MQTT_BUS_CALLBACK_WORKERS: ${MQTT_BUS_CALLBACK_WORKERS:-32}
      BROKER_TYPE: ${BROKER_TYPE:-mqtt}
      MQTT_BROKER: ${MQTT_BROKER:-mosquitto}
      MQTT_PORT: ${MQTT_PORT:-1883}
      KAFKA_BOOTSTRAP_SERVERS: kafka:29092
      BROKER_USER: ${ADMIN_USER:-admin}
      BROKER_PASSWORD: ${ADMIN_PASSWORD:-admin123}
      PYTHONUNBUFFERED: 1
      ORVD_TOPIC: ${ORVD_TOPIC:-}
      NUS_TOPIC: ${NUS_TOPIC:-}
      DRONEPORT_TOPIC: ${DRONEPORT_TOPIC:-}
      SITL_TOPIC: ${SITL_TOPIC:-}
      SITL_COMMANDS_TOPIC: ${SITL_COMMANDS_TOPIC:-}
      SITL_TELEMETRY_REQUEST_TOPIC: ${SITL_TELEMETRY_REQUEST_TOPIC:-}
      COMPONENT_ID: ${NAVIGATION_COMPONENT_ID:-navigation}
      NAVIGATION_POLL_INTERVAL_S: ${NAVIGATION_NAVIGATION_POLL_INTERVAL_S:-0.1}
      NAVIGATION_REQUEST_TIMEOUT_S: ${NAVIGATION_NAVIGATION_REQUEST_TIMEOUT_S:-1.0}
      SITL_DRONE_ID: ${NAVIGATION_SITL_DRONE_ID:-drone_001}
    networks:
    - drones_net
    restart: unless-stopped
    depends_on:
      kafka:
        condition: service_healthy
        required: false
      mosquitto:
        condition: service_healthy
        required: false
  autopilot:
    build:
      context: ../..
      dockerfile: agrodron/components/autopilot/docker/Dockerfile
    profiles:
    - kafka
    - mqtt
    environment:
      TOPIC_VERSION: ${TOPIC_VERSION:-v1}
      SYSTEM_NAME: ${SYSTEM_NAME:-Agrodron}
      INSTANCE_ID: ${INSTANCE_ID:-Agrodron001}
      MQTT_BUS_CALLBACK_WORKERS: ${MQTT_BUS_CALLBACK_WORKERS:-32}
      BROKER_TYPE: ${BROKER_TYPE:-mqtt}
      MQTT_BROKER: ${MQTT_BROKER:-mosquitto}
      MQTT_PORT: ${MQTT_PORT:-1883}
      KAFKA_BOOTSTRAP_SERVERS: kafka:29092
      BROKER_USER: ${ADMIN_USER:-admin}
      BROKER_PASSWORD: ${ADMIN_PASSWORD:-admin123}
      PYTHONUNBUFFERED: 1
      ORVD_TOPIC: ${ORVD_TOPIC:-}
      NUS_TOPIC: ${NUS_TOPIC:-}
      DRONEPORT_TOPIC: ${DRONEPORT_TOPIC:-}
      SITL_TOPIC: ${SITL_TOPIC:-}
      SITL_COMMANDS_TOPIC: ${SITL_COMMANDS_TOPIC:-}
      SITL_TELEMETRY_REQUEST_TOPIC: ${SITL_TELEMETRY_REQUEST_TOPIC:-}
      COMPONENT_ID: ${AUTOPILOT_COMPONENT_ID:-autopilot}
      AUTOPILOT_CONTROL_INTERVAL_S: ${AUTOPILOT_AUTOPILOT_CONTROL_INTERVAL_S:-0.2}
      AUTOPILOT_NAV_POLL_INTERVAL_S: ${AUTOPILOT_AUTOPILOT_NAV_POLL_INTERVAL_S:-0.2}
      AUTOPILOT_REQUEST_TIMEOUT_S: ${AUTOPILOT_AUTOPILOT_REQUEST_TIMEOUT_S:-2.0}
      AUTOPILOT_ORVD_MOCK_SUCCESS: ${AUTOPILOT_AUTOPILOT_ORVD_MOCK_SUCCESS:-0}
      AUTOPILOT_DRONEPORT_MOCK_SUCCESS: ${AUTOPILOT_AUTOPILOT_DRONEPORT_MOCK_SUCCESS:-0}
    networks:
    - drones_net
    restart: unless-stopped
    depends_on:
      kafka:
        condition: service_healthy
        required: false
      mosquitto:
        condition: service_healthy
        required: false
  limiter:
    build:
      context: ../..
      dockerfile: agrodron/components/limiter/docker/Dockerfile
    profiles:
    - kafka
    - mqtt
    environment:
      TOPIC_VERSION: ${TOPIC_VERSION:-v1}
      SYSTEM_NAME: ${SYSTEM_NAME:-Agrodron}
      INSTANCE_ID: ${INSTANCE_ID:-Agrodron001}
      MQTT_BUS_CALLBACK_WORKERS: ${MQTT_BUS_CALLBACK_WORKERS:-32}
      BROKER_TYPE: ${BROKER_TYPE:-mqtt}
      MQTT_BROKER: ${MQTT_BROKER:-mosquitto}
      MQTT_PORT: ${MQTT_PORT:-1883}
      KAFKA_BOOTSTRAP_SERVERS: kafka:29092
      BROKER_USER: ${ADMIN_USER:-admin}
      BROKER_PASSWORD: ${ADMIN_PASSWORD:-admin123}
      PYTHONUNBUFFERED: 1
      ORVD_TOPIC: ${ORVD_TOPIC:-}
      NUS_TOPIC: ${NUS_TOPIC:-}
      DRONEPORT_TOPIC: ${DRONEPORT_TOPIC:-}
      SITL_TOPIC: ${SITL_TOPIC:-}
      SITL_COMMANDS_TOPIC: ${SITL_COMMANDS_TOPIC:-}
      SITL_TELEMETRY_REQUEST_TOPIC: ${SITL_TELEMETRY_REQUEST_TOPIC:-}
      COMPONENT_ID: ${LIMITER_COMPONENT_ID:-limiter}
      LIMITER_CONTROL_INTERVAL_S: ${LIMITER_LIMITER_CONTROL_INTERVAL_S:-0.5}
      LIMITER_NAV_POLL_INTERVAL_S: ${LIMITER_LIMITER_NAV_POLL_INTERVAL_S:-0.5}
      LIMITER_TELEMETRY_POLL_INTERVAL_S: ${LIMITER_LIMITER_TELEMETRY_POLL_INTERVAL_S:-1.0}
      LIMITER_REQUEST_TIMEOUT_S: ${LIMITER_LIMITER_REQUEST_TIMEOUT_S:-2.0}
      LIMITER_MAX_DISTANCE_FROM_PATH_M: ${LIMITER_LIMITER_MAX_DISTANCE_FROM_PATH_M:-10.0}
      LIMITER_MAX_ALT_DEVIATION_M: ${LIMITER_LIMITER_MAX_ALT_DEVIATION_M:-3.0}
    networks:
    - drones_net
    restart: unless-stopped
    depends_on:
      kafka:
        condition: service_healthy
        required: false
      mosquitto:
        condition: service_healthy
        required: false
  emergensy:
    build:
      context: ../..
      dockerfile: agrodron/components/emergensy/docker/Dockerfile
    profiles:
    - kafka
    - mqtt
    environment:
      TOPIC_VERSION: ${TOPIC_VERSION:-v1}
      SYSTEM_NAME: ${SYSTEM_NAME:-Agrodron}
      INSTANCE_ID: ${INSTANCE_ID:-Agrodron001}
      MQTT_BUS_CALLBACK_WORKERS: ${MQTT_BUS_CALLBACK_WORKERS:-32}
      BROKER_TYPE: ${BROKER_TYPE:-mqtt}
      MQTT_BROKER: ${MQTT_BROKER:-mosquitto}
      MQTT_PORT: ${MQTT_PORT:-1883}
      KAFKA_BOOTSTRAP_SERVERS: kafka:29092
      BROKER_USER: ${ADMIN_USER:-admin}
      BROKER_PASSWORD: ${ADMIN_PASSWORD:-admin123}
      PYTHONUNBUFFERED: 1
      ORVD_TOPIC: ${ORVD_TOPIC:-}
      NUS_TOPIC: ${NUS_TOPIC:-}
      DRONEPORT_TOPIC: ${DRONEPORT_TOPIC:-}
      SITL_TOPIC: ${SITL_TOPIC:-}
      SITL_COMMANDS_TOPIC: ${SITL_COMMANDS_TOPIC:-}
      SITL_TELEMETRY_REQUEST_TOPIC: ${SITL_TELEMETRY_REQUEST_TOPIC:-}
      COMPONENT_ID: ${EMERGENSY_COMPONENT_ID:-emergensy}
      EMERGENSY_PUBLISH_TIMEOUT_S: ${EMERGENSY_EMERGENSY_PUBLISH_TIMEOUT_S:-1.0}
    networks:
    - drones_net
    restart: unless-stopped
    depends_on:
      kafka:
        condition: service_healthy
        required: false
      mosquitto:
        condition: service_healthy
        required: false
  mission_handler:
    build:
      context: ../..
      dockerfile: agrodron/components/mission_handler/docker/Dockerfile
    profiles:
    - kafka
    - mqtt
    environment:
      TOPIC_VERSION: ${TOPIC_VERSION:-v1}
      SYSTEM_NAME: ${SYSTEM_NAME:-Agrodron}
      INSTANCE_ID: ${INSTANCE_ID:-Agrodron001}
      MQTT_BUS_CALLBACK_WORKERS: ${MQTT_BUS_CALLBACK_WORKERS:-32}
      BROKER_TYPE: ${BROKER_TYPE:-mqtt}
      MQTT_BROKER: ${MQTT_BROKER:-mosquitto}
      MQTT_PORT: ${MQTT_PORT:-1883}
      KAFKA_BOOTSTRAP_SERVERS: kafka:29092
      BROKER_USER: ${ADMIN_USER:-admin}
      BROKER_PASSWORD: ${ADMIN_PASSWORD:-admin123}
      PYTHONUNBUFFERED: 1
      ORVD_TOPIC: ${ORVD_TOPIC:-}
      NUS_TOPIC: ${NUS_TOPIC:-}
      DRONEPORT_TOPIC: ${DRONEPORT_TOPIC:-}
      SITL_TOPIC: ${SITL_TOPIC:-}
      SITL_COMMANDS_TOPIC: ${SITL_COMMANDS_TOPIC:-}
      SITL_TELEMETRY_REQUEST_TOPIC: ${SITL_TELEMETRY_REQUEST_TOPIC:-}
      COMPONENT_ID: ${MISSION_HANDLER_COMPONENT_ID:-mission_handler}
      MISSION_HANDLER_REQUEST_TIMEOUT_S: ${MISSION_HANDLER_MISSION_HANDLER_REQUEST_TIMEOUT_S:-10.0}
      SITL_DRONE_ID: ${MISSION_HANDLER_SITL_DRONE_ID:-drone_001}
    networks:
    - drones_net
    restart: unless-stopped
    depends_on:
      kafka:
        condition: service_healthy
        required: false
      mosquitto:
        condition: service_healthy
        required: false
  motors:
    build:
      context: ../..
      dockerfile: agrodron/components/motors/docker/Dockerfile
    profiles:
    - kafka
    - mqtt
    environment:
      TOPIC_VERSION: ${TOPIC_VERSION:-v1}
      SYSTEM_NAME: ${SYSTEM_NAME:-Agrodron}
      INSTANCE_ID: ${INSTANCE_ID:-Agrodron001}
      MQTT_BUS_CALLBACK_WORKERS: ${MQTT_BUS_CALLBACK_WORKERS:-32}
      BROKER_TYPE: ${BROKER_TYPE:-mqtt}
      MQTT_BROKER: ${MQTT_BROKER:-mosquitto}
      MQTT_PORT: ${MQTT_PORT:-1883}
      KAFKA_BOOTSTRAP_SERVERS: kafka:29092
      BROKER_USER: ${ADMIN_USER:-admin}
      BROKER_PASSWORD: ${ADMIN_PASSWORD:-admin123}
      PYTHONUNBUFFERED: 1
      ORVD_TOPIC: ${ORVD_TOPIC:-}
      NUS_TOPIC: ${NUS_TOPIC:-}
      DRONEPORT_TOPIC: ${DRONEPORT_TOPIC:-}
      SITL_TOPIC: ${SITL_TOPIC:-}
      SITL_COMMANDS_TOPIC: ${SITL_COMMANDS_TOPIC:-}
      SITL_TELEMETRY_REQUEST_TOPIC: ${SITL_TELEMETRY_REQUEST_TOPIC:-}
      COMPONENT_ID: ${MOTORS_COMPONENT_ID:-motors}
      SITL_MODE: ${MOTORS_SITL_MODE:-mock}
      SITL_DRONE_ID: ${MOTORS_SITL_DRONE_ID:-drone_001}
      MOTORS_TEMPERATURE_C_DEFAULT: ${MOTORS_MOTORS_TEMPERATURE_C_DEFAULT:-55.0}
    networks:
    - drones_net
    restart: unless-stopped
    depends_on:
      kafka:
        condition: service_healthy
        required: false
      mosquitto:
        condition: service_healthy
        required: false
  sprayer:
    build:
      context: ../..
      dockerfile: agrodron/components/sprayer/docker/Dockerfile
    profiles:
    - kafka
    - mqtt
    environment:
      TOPIC_VERSION: ${TOPIC_VERSION:-v1}
      SYSTEM_NAME: ${SYSTEM_NAME:-Agrodron}
      INSTANCE_ID: ${INSTANCE_ID:-Agrodron001}
      MQTT_BUS_CALLBACK_WORKERS: ${MQTT_BUS_CALLBACK_WORKERS:-32}
      BROKER_TYPE: ${BROKER_TYPE:-mqtt}
      MQTT_BROKER: ${MQTT_BROKER:-mosquitto}
      MQTT_PORT: ${MQTT_PORT:-1883}
      KAFKA_BOOTSTRAP_SERVERS: kafka:29092
      BROKER_USER: ${ADMIN_USER:-admin}
      BROKER_PASSWORD: ${ADMIN_PASSWORD:-admin123}
      PYTHONUNBUFFERED: 1
      ORVD_TOPIC: ${ORVD_TOPIC:-}
      NUS_TOPIC: ${NUS_TOPIC:-}
      DRONEPORT_TOPIC: ${DRONEPORT_TOPIC:-}
      SITL_TOPIC: ${SITL_TOPIC:-}
      SITL_COMMANDS_TOPIC: ${SITL_COMMANDS_TOPIC:-}
      SITL_TELEMETRY_REQUEST_TOPIC: ${SITL_TELEMETRY_REQUEST_TOPIC:-}
      COMPONENT_ID: ${SPRAYER_COMPONENT_ID:-sprayer}
      SITL_MODE: ${SPRAYER_SITL_MODE:-mock}
      SPRAYER_TEMPERATURE_C_DEFAULT: ${SPRAYER_SPRAYER_TEMPERATURE_C_DEFAULT:-40.0}
      SPRAYER_TANK_LEVEL_PCT_DEFAULT: ${SPRAYER_SPRAYER_TANK_LEVEL_PCT_DEFAULT:-75.0}
    networks:
    - drones_net
    restart: unless-stopped
    depends_on:
      kafka:
        condition: service_healthy
        required: false
      mosquitto:
        condition: service_healthy
        required: false
  telemetry:
    build:
      context: ../..
      dockerfile: agrodron/components/telemetry/docker/Dockerfile
    profiles:
    - kafka
    - mqtt
    environment:
      TOPIC_VERSION: ${TOPIC_VERSION:-v1}
      SYSTEM_NAME: ${SYSTEM_NAME:-Agrodron}
      INSTANCE_ID: ${INSTANCE_ID:-Agrodron001}
      MQTT_BUS_CALLBACK_WORKERS: ${MQTT_BUS_CALLBACK_WORKERS:-32}
      BROKER_TYPE: ${BROKER_TYPE:-mqtt}
      MQTT_BROKER: ${MQTT_BROKER:-mosquitto}
      MQTT_PORT: ${MQTT_PORT:-1883}
      KAFKA_BOOTSTRAP_SERVERS: kafka:29092
      BROKER_USER: ${ADMIN_USER:-admin}
      BROKER_PASSWORD: ${ADMIN_PASSWORD:-admin123}
      PYTHONUNBUFFERED: 1
      ORVD_TOPIC: ${ORVD_TOPIC:-}
      NUS_TOPIC: ${NUS_TOPIC:-}
      DRONEPORT_TOPIC: ${DRONEPORT_TOPIC:-}
      SITL_TOPIC: ${SITL_TOPIC:-}
      SITL_COMMANDS_TOPIC: ${SITL_COMMANDS_TOPIC:-}
      SITL_TELEMETRY_REQUEST_TOPIC: ${SITL_TELEMETRY_REQUEST_TOPIC:-}
      COMPONENT_ID: ${TELEMETRY_COMPONENT_ID:-telemetry}
      TELEMETRY_POLL_INTERVAL_S: ${TELEMETRY_TELEMETRY_POLL_INTERVAL_S:-0.5}
      TELEMETRY_REQUEST_TIMEOUT_S: ${TELEMETRY_TELEMETRY_REQUEST_TIMEOUT_S:-6.0}
    networks:
    - drones_net
    restart: unless-stopped
    depends_on:
      kafka:
        condition: service_healthy
        required: false
      mosquitto:
        condition: service_healthy
        required: false
  system_monitor:
    build:
      context: ../..
      dockerfile: agrodron/components/system_monitor/docker/Dockerfile
    profiles:
    - kafka
    - mqtt
    environment:
      TOPIC_VERSION: ${TOPIC_VERSION:-v1}
      SYSTEM_NAME: ${SYSTEM_NAME:-Agrodron}
      INSTANCE_ID: ${INSTANCE_ID:-Agrodron001}
      MQTT_BUS_CALLBACK_WORKERS: ${MQTT_BUS_CALLBACK_WORKERS:-32}
      BROKER_TYPE: ${BROKER_TYPE:-mqtt}
      MQTT_BROKER: ${MQTT_BROKER:-mosquitto}
      MQTT_PORT: ${MQTT_PORT:-1883}
      KAFKA_BOOTSTRAP_SERVERS: kafka:29092
      BROKER_USER: ${ADMIN_USER:-admin}
      BROKER_PASSWORD: ${ADMIN_PASSWORD:-admin123}
      PYTHONUNBUFFERED: 1
      ORVD_TOPIC: ${ORVD_TOPIC:-}
      NUS_TOPIC: ${NUS_TOPIC:-}
      DRONEPORT_TOPIC: ${DRONEPORT_TOPIC:-}
      SITL_TOPIC: ${SITL_TOPIC:-}
      SITL_COMMANDS_TOPIC: ${SITL_COMMANDS_TOPIC:-}
      SITL_TELEMETRY_REQUEST_TOPIC: ${SITL_TELEMETRY_REQUEST_TOPIC:-}
      COMPONENT_ID: ${SYSTEM_MONITOR_COMPONENT_ID:-system_monitor}
      SYSTEM_MONITOR_HTTP: ${SYSTEM_MONITOR_SYSTEM_MONITOR_HTTP:-1}
      SYSTEM_MONITOR_HTTP_PORT: ${SYSTEM_MONITOR_SYSTEM_MONITOR_HTTP_PORT:-8090}
      SYSTEM_MONITOR_TELEMETRY_TIMEOUT_S: ${SYSTEM_MONITOR_SYSTEM_MONITOR_TELEMETRY_TIMEOUT_S:-15.0}
    ports:
    - ${SYSTEM_MONITOR_HOST_PORT:-8090}:${SYSTEM_MONITOR_SYSTEM_MONITOR_HTTP_PORT:-8090}
    healthcheck:
      test:
      - CMD-SHELL
      - curl -fsS http://127.0.0.1:$${SYSTEM_MONITOR_HTTP_PORT}/health || exit 1
      interval: 15s
      timeout: 5s
      retries: 3
      start_period: 10s
    networks:
    - drones_net
    restart: unless-stopped
    depends_on:
      kafka:
        condition: service_healthy
        required: false
      mosquitto:
        condition: service_healthy
        required: false
networks:
  drones_net:
    driver: bridge
    name: ${DOCKER_NETWORK:-drones_net}
