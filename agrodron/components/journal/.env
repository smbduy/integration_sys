COMPONENT_ID=journal

# По умолчанию: components/journal/system_journal/system_<UTC>.ndjson (новый файл на каждый запуск).
# JOURNAL_LOG_DIR=/path/to/dir — другой каталог для таких файлов.
# JOURNAL_FILE_PATH=/path/file.ndjson — один фиксированный файл (перекрывает режим выше).
