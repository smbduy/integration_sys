[project]
name = "sbd-drones-economics"
version = "0.1.0"
description = "SBD drones economics — broker + SDK"
readme = "README.md"
requires-python = ">=3.12"

[tool.pytest.ini_options]
pythonpath = [".."]
testpaths = ["../tests", "../dummy_component", "../dummy_system"]
addopts = "-v"
